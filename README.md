pinboard-scripts
================

scripts to manage pinboard bookmarks


files
===
add login credentials to auth.lua with the following:

	auth = {
		username = "username",
		password = "password"
	}

